# Distribution Manifests

Create one manifest directory per day:

```text
distribution_runtime/manifests/YYYY-MM-DD/distribution_manifest.json
```

The manifest records which queue slot directories were actually assembled.
